export type ProgressHeaderProps = {
  percentage?: number;
  primary?: boolean;
  title?: string;
  useBackButton?: boolean;
  noSeparator?: boolean;
  hideBurgerButton?: boolean;
  onMenuClick?: () => {};
};
